# Clé API Groq (https://console.groq.com -> API Keys -> Create API Key)
# Utilisée uniquement côté serveur par api/chat.js — ne jamais préfixer par VITE_
# (sinon elle serait exposée dans le bundle client).
GROQ_API_KEY=
